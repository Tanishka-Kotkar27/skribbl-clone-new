# Loaded automatically by `npm run dev`. Points the app at the local backend,
# so no .env.local is needed for development. Production values are set as
# environment variables on the hosting platform instead (see README).
VITE_API_BASE_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws
